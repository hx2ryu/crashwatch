{
  "name": "@crashwatch/provider-firebase",
  "version": "0.1.0-alpha.0",
  "description": "Firebase Crashlytics provider for crashwatch. BigQuery-export-based (stable) with a CLI-wrapper fallback for small installs.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "@crashwatch/core": "workspace:*"
  }
}
