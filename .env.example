# Copy to .env and fill in. crashwatch does not auto-load .env; use a runner
# like `direnv`, `dotenv`, or your shell's native support.

# Generic HTTP destination
CRASHWATCH_WEBHOOK_URL=

# Slack Incoming Webhook
SLACK_WEBHOOK_URL=

# Google service account (for Firebase provider)
GOOGLE_APPLICATION_CREDENTIALS=
